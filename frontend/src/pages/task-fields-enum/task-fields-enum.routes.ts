import { Routes } from '@angular/router';
import { TaskFieldsEnumComponent } from './task-fields-enum.component';

export const TASK_FIELDS_ENUM_ROUTES: Routes = [
    { path: '', component: TaskFieldsEnumComponent },
];
