import { Routes } from '@angular/router';
import { AssigneeEnumComponent } from './assignee-enum.component';

export const ASSIGNEE_ENUM_ROUTES: Routes = [
    { path: '', component: AssigneeEnumComponent },
];
