import { Routes } from '@angular/router';
import { StatusEnumComponent } from './status-enum.component';

export const STATUS_ENUM_ROUTES: Routes = [
    { path: '', component: StatusEnumComponent },
];
